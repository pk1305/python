import pandas as pd

# List of tuples
data = [(1, 'Alice', 25), (2, 'Bob', 30), (3, 'Charlie', 22)]

# Creating DataFrame
df = pd.DataFrame(data, columns=['ID', 'Name', 'Age'])

# Display DataFrame
print(df)
